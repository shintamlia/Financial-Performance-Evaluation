-- url dataset
-- https://www.kaggle.com/datasets/dsfelix/us-stores-sales

-- create server named project_m3
create server project_m3

-- create database named postgres
create database postgres

-- create table named table_m3
create table if not exists table_m3(
	"Area Code" integer,
	"State" varchar(50),
    "Market" varchar(25),
    "Market Size" varchar(50),
    "Profit" numeric,
    "Margin" numeric,
    "Sales" numeric,
    "COGS" numeric,
    "Total Expenses" numeric,
    "Marketing" numeric,
	"Inventory" numeric,
    "Budget Profit" numeric,
	"Budget COGS" numeric,
	"Budget Margin" numeric,
	"Budget Sales" numeric,
    "ProductId" integer,
    "Date" date,
    "Product Type" varchar(25),
    "Product" varchar(25),
    "Type" varchar(25)
);

-- insert Data with import data 
